# Requirements

- Continue the previous lower-page UI visual refinement.
- Bring camera list, remote, confirm, and message pages into the same title/content/card spacing system already applied to scanning, GPS, and connecting pages.
- Keep existing behavior and touch/key logic unchanged unless layout requires a matching hit-line update.
- Preserve informative footer text where still useful (for example the remote page hint) unless the user explicitly asked to remove it.
- Verify with both PlatformIO environments after changes.
